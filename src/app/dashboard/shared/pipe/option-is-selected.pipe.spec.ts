import { OptionIsSelectedPipe } from './option-is-selected.pipe';

describe('OptionIsSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionIsSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
