import { DisableWidgetListPipe } from './disable-widget-list.pipe';

describe('DisableWidgetListPipe', () => {
  it('create an instance', () => {
    const pipe = new DisableWidgetListPipe();
    expect(pipe).toBeTruthy();
  });
});
