import { LabelToArrayPipe } from './label-to-array.pipe';

describe('LabelToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
