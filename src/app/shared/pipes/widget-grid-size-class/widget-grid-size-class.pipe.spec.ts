import { WidgetGridSizeClassPipe } from './widget-grid-size-class.pipe';

describe('WidgetGridSizeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new WidgetGridSizeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
